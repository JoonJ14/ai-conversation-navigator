# Review Pipeline — 2026-07-30 — feat/summary-export-fixtures @ 0a63780

**Base:** main (`ddfeb9b`) · **Diff:** 386 committed + ~135 uncommitted lines, 8 files
(userscript live code + test harness + 6 docs) · **Tiers:** all three (50+ lines, live code)
**Tier 3 backend:** opus (5 lenses + skeptic verification, per `_adversarial-procedure.md`)

## Tier 1 — Structural (PASS after fixes)

| File | Result |
|---|---|
| ai-conversation-navigator.user.js | PASS — additive contract attrs; both version sites bumped; `startIdx/endIdx` present on all segment construction paths incl. merges; no selector collision (sub-segments unattributed); ES5-clean |
| tests/test-all-platforms.js | PASS after 1 WARN + 1 NOTE fixed (below) |

## Tier 2 — Deep logic (PASS after fixes)

| Sev | Finding | Fix |
|---|---|---|
| WARN | S3's leading comment described the `busySeen` discriminator that the corrected assertion no longer uses — comment-vs-code disagreement | Comment rewritten to match the assertion |
| NOTE | E1's evaluate kept a dead `expUserN` parameter from a replaced readiness probe | Parameter and argument removed |

Cross-path checks clean: `__acnTestDownloads` reset per capture; contract attrs re-set on every
render; toast singleton; no reader of message text can reach panel/toast attributes; per-entry
page rebuild prevents cross-entry prototype-patch bleed.

## Tier 3 — opus lens round 1 (IN PROGRESS)

### Lens: security / blast radius — reported, findings verified by operator against source

| Sev | Finding | Verdict |
|---|---|---|
| WARN | E1 readiness probe checks `data-acn-index-status` on the ZONE; the userscript sets it on the nav-panel BANNER (userscript:5681/5687/5704) — probe always passes in 0ms; E1's stability was the coincidence 350ms < the 500ms post-pushState rescan delay; under load it exports the stale (other-conversation) index. Inherited from agent_docs/testing.md's incorrect "On zone" row | **CONFIRMED** — fix at consolidation |
| WARN | `DOWNLOAD_SHIM` patches `HTMLAnchorElement.prototype.click` on all 25 entries; ungated (gmShim is gated); swallows ANY download-attr anchor incl. `downloadImage()`'s | **CONFIRMED** — gate on the two flags |
| NOTE | `blobByUrl` strong-refs every Blob; `revokeObjectURL` unpatched → frees nothing | fix: patch revoke to delete key |
| NOTE | Capture is page-realm-scoped; cannot detect sandbox-realm export breaks (DEC-019/020 context split); TESTING.md overstates "identical on all engines" | fix: scope statement in TESTING.md |
| NOTE | E2 bounds 4..12 hardcoded, not derived from `platform.virtualized.userWindowSize` | fix: derive |
| NOTE | E1/E2 click the Tools dot unconditionally; `orbOpenPanel` toggles-closed if already open (userscript:5584) | **CONFIRMED** — guard |

### Full-suite corroboration

The both-engine full run (pre-Tier-fix code) failed **3 tests on 1 platform** (1041/1044) while
the same entries pass in isolation on both engines — consistent with the no-op-probe race under
full-suite CPU load. Re-run scheduled after consolidation fixes.

### Lens: caller contracts / value plumbing — reported

| Sev | Finding | Verdict |
|---|---|---|
| CRITICAL | **E2 never executes**: its guard sits INSIDE `if (platform.indexBacked)`; the only `degradedExportTest` entry is deliberately not index-backed. Verified: the assertion never appears in the degraded entry's output. Docs claim coverage that does not exist | **CONFIRMED** — hoist the block; correct the coverage claims |
| CRITICAL | When E2 is made reachable it fails on the em-dash: `buildTestPage` emits no `<meta charset>` and the route sets no charset, so the inlined userscript's literal U+2014 decodes as windows-1252 mojibake. Lens verified empirically with a hoisted copy. Same mechanism visible in S4's toast text (`â€”`) — observed and wrongly dismissed as terminal rendering | **CONFIRMED** — add charset to the harness page + route |
| WARN | Same readiness-probe no-op as the security lens, PLUS: even a document-wide banner query would not help — the banner only refreshes while the Navigate panel is open (`orbPopulateNavigate` gated on `orbPanel==='nav'`) | **CONFIRMED** — replace with the GM shim's own `__convFetches` counter (harness-owned, panel-independent) |
| NOTE | E2's `headerN === sections` is tautological — both sides derive from the same `timeline` array | remove; keep the derived bounds |
| NOTE | E2 bounds hardcoded (dup of security lens #5) | derive from `userWindowSize` |
| NOTE | `sum-results` and `data-acn-sum-end` have no reader; `[data-acn-sum-start="0"]` selects the only segment on this fixture | scope S1 via sum-results; docs say "unreferenced as yet" for sum-end; fixture comment notes single-segment |
| NOTE | Blob retention (dup of security lens #3) | patch revokeObjectURL |
| NOTE | Latent coupling: `expPath` formula vs `stats.totalMessages` composition diverge if a provisional turn ever exists on this fixture | fixture comment |

**Process incident:** a lens wrote an untracked probe (`tests/.acn-e2-probe.js`) into the repo
tree — the same §J violation as v12.1. To be deleted at consolidation; explicit `git add`
paths remain the rule.

### Lens: correctness — reported

Corroborates both CRITICALs (E2 unreachable; charset mojibake — verified `document.characterSet === "windows-1252"`) and the readiness no-op WARN, with the refinement that the wrong-conversation export is structurally undetectable on this fixture (shim serves one payload for every uuid) — only the mid-reload DEGRADED shape is catchable, so the wait must still be made real. New items:

| Sev | Finding | Verdict |
|---|---|---|
| WARN | E2's `headerN === sections` unfailable (tautology); the DOM path's REAL emittable inconsistency is header total vs its own `(a user, b AI)` parenthetical when `buildTimeline` drops null-element entries — uncompared | **CONFIRMED** — assert `N === a + b` instead |
| NOTE | `exportSummary`/`exportBookmarks` have no failure handling; a throw escapes uncaught with no toast (sibling has one) | fix live: guard `tool.action()` |
| NOTE | `exportSummary` labels timeline spans as `Q1–Q81` — globalIdx over the combined timeline mislabeled as question numbers | fix live: `msgs 1–81` (panel's own convention) |
| NOTE | `gmf` dereferenced unguarded; odd `totalMessages` yields `40.5 user` regex; alternation assumption uncommented | guard + comment |
| NOTE | Zone lookups unguarded in 4 evaluates (TypeError aborts the entry instead of one FAIL) | null-guard |
| NOTE | Blob retention (3rd duplicate) | patch revoke |

### Lens: test-gate integrity — reported (both CRITICALs re-confirmed with byte-level evidence)

| Sev | Finding | Verdict |
|---|---|---|
| WARN | S1 cannot gate `sum-generate`: the panel AUTO-generates 50ms after open (userscript:5627-32), so stats pre-exist the test's click. Mutation-verified: attribute moved onto the disclaimer div → still 34/34 | **CONFIRMED** — add a regenerate assertion (old stats node identity must be replaced by clicking the attributed button) |
| WARN | S4 gates "stamp changed", not "conversation changed": invalidation nulls the stamp and every rebuild bumps the global gen, so dropping the conversation-id half passes. Mutation-verified | **CONFIRMED as scoped-coverage** — the id half is defense-in-depth (gen is globally monotonic; no reachable scenario isolates id); document the scope honestly rather than fake a gate |
| WARN | Single-segment fixture: `sum-start/end` carry no signal; segmentation collapse passes S1–S4 | **CONFIRMED** — assert the segment's `data-acn-sum-end === expPath-1` (spans the whole timeline; makes sum-end load-bearing); multi-segment fixture recorded as follow-up debt |
| NOTE | `resolved = +raw` coerces empty attr to 0 | guard |
| NOTE | S2's rowText conjunct non-discriminating given resolved===0 | keep as belt; no claim |
| NOTE | E3 lacks E1's readiness protection; also couples to E1's panel state | give E3 the same counter-based wait |
| NOTE | Degraded-session summary paths (`_sumElKey` staleness branch, sub-segment + inventory click handlers) remain unexecuted | recorded debt (follow-up batch) |
| NOTE | No per-entry assertion-count check — THE mechanism that let unreachable-E2 ship green | **fix structurally**: per-flag expected-assertion-title presence check after each entry |

### Lens: lifecycle — reported (the deepest mechanism finding of the round)

| Sev | Finding | Verdict |
|---|---|---|
| CRITICAL | (dup) E2 unreachable — 5/5 lenses | CONFIRMED |
| CRITICAL | **S4's conversation switch never happened**: claude is `spa:false` (no pushState proxy), an idle mock emits no mutations, so no scan ever ran — measured `__convFetches` 1 before and 1 after. The refusal fired via "index not ready" (URL≠index id → stamp null), not the live-vs-live comparison the test names. Also explains the green/red flip across runs: residual scroll mutations made scans opportunistic | **CONFIRMED** — switch made real (scroll-nudged, counter-verified both ways) |
| CRITICAL | (dup) E1 readiness no-op; retry equally dead; comment and doc row describe behaviour the code does not have | CONFIRMED — counter-based wait |
| WARN | S1/E1 leave `orbPanel` on summary/tools, freezing the nav list for pre-existing TESTS 23-27 (measured: 40/40 stamped nodes surviving to TEST 26) | **CONFIRMED** — post-block panel-state restore |
| WARN | S2 jump cap 6500ms vs 11000ms for the same jump in TESTS 23/24 — the webkit/macos cap-ceiling shape | **CONFIRMED** — aligned to 11s |
| NOTE | (dups) shim global patch + blob retention; single-segment fixture; unread attributes | fixed / scoped as recorded |

## Consolidation — round 1 totals

**Raw findings across 5 lenses:** 31 (heavy overlap — E2's unreachability was found by 5/5,
the charset CRITICAL by 3/5, the readiness no-op by 4/5). **After dedup: 17 distinct.**
**Verified real: 17** (several empirically, by mutation or measurement, by the lenses
themselves; the operator source-verified the rest). **False positives: 0.**

**All 17 fixed** in commit `4e43f71` except three recorded-as-scope items (S4's stamp-id
half is defense-in-depth the fixture cannot isolate; the single-segment fixture leaves
multi-segment segmentation unexercised; degraded-session summary paths remain debt) — each
written into the fixture comments and TESTING.md rather than silently accepted.

**Process incidents, on the record:** (1) a lens wrote an untracked probe into the repo
tree (removed; the v12.1 §J violation repeated); (2) the operator ran `git checkout`
mutation-restores against UNCOMMITTED fixes — twice in one session — wiping the Tier 2/3
fixes mid-verification and invalidating one 4-mutant run. Everything was re-applied and
re-verified against a commit. Rule now stated in the commit itself: **commit before
mutating.**

**Post-fix validation:** 84/84 across the three virtualized entries (E2 present for the
first time), all four dead-zone mutants re-verified red individually against `4e43f71`,
regenerate-gate and presence-check mutations red, tree clean after restores.

## Round 2 — post-fix validation (3 lenses on commit 4e43f71)

### Lens: test-gate — reported. Both round-1 CRITICAL fixes independently CONFIRMED
(E2 reachable at the virtualized level; presence titles byte-exact vs assert titles,
all emitted on error paths too; charset clean). No new CRITICAL. In the fixes:

| Sev | Finding | Disposition |
|---|---|---|
| WARN | `__convFetches` is uuid-blind — S4's "counter-verified switch" claim exceeds the observable (a same-conversation resync also increments it) | fix: shim records `__convLastUuid`; S4 asserts the uuid both ways |
| WARN | `segEndMax` measured pre-regenerate; on regenerate failure the stats fields read a DETACHED node | fix: hard-fail when not regenerated; read everything post-regenerate |
| NOTE | `segEndMax === expPath-1` is structurally forced (every construction path ends at timeline.length-1) — the same tautology class round 1 removed | fix: demote to detail-string info; docs corrected |
| NOTE | Regenerate gate satisfiable by the FIRST click when the 40ms re-enable slips past the poll (disabled-button click is swallowed) | fix: wait for `!gen.disabled` before the second click |
| NOTE | E2 lo bound (uws+1=4) sits below the healthy window (~7); hi identical to the old hardcode | fix: derive 2·uws .. 2·uws+4 |
| NOTE | S2 whitespace still coerces to 0 (TEST 23 uses /^\d+$/) | fix: same regex |
| NOTE | S3 missing the zone guard S2/S4 got; a null zone bypasses the presence check via the outer catch | fix: guard |
| NOTE | Readiness wait gives up silently after 7.2s | fix: report the wait-out in err |
| NOTE | tool.action() catch removes the suite's only detection channel for exportBookmarks throws | keep the guard (live UX correct); debt note updated: a future fixture must assert toast/file, not pageerror |
| NOTE | S4 conflates refusal with restore bookkeeping in one assert | fix: split |
| NOTE | E2's page-state restore skipped on err paths | fix: unconditional second evaluate |

### Round 2, lenses 2+3 (correctness, lifecycle) — both round-1 CRITICAL fixes re-confirmed
(one lens re-ran the suite green before reviewing; the other mutation/ablation-verified).
Additional round-2 findings, deduped across all three lenses:

| Sev | Finding | Fix |
|---|---|---|
| WARN | E2's replacement check ALSO tautological on its entry (degraded DOM scan cannot produce falsy elements → buildTimeline drops nothing; MUTATION-VERIFIED unfailable) and the "derived" bounds were numerically the old hardcodes around a deterministic value | exact `2·uws+1` expectation; parts check demoted to cross-check; docs corrected |
| WARN | S1 mixed two renders in one assertion (segEndMax from pre-regenerate DOM; detached-node reads on failure) | all reads post-regenerate; hard-fail on non-replacement; segEndMax demoted to info (itself structurally forced) |
| WARN | `__convFetches` uuid-blind (increment at request START, same payload per uuid); restore ABLATION-VERIFIED non-load-bearing | shim records `__convLastUuid`; S4 waits on the uuid both ways; refusal and bookkeeping split into two assertions; honesty limits recorded |
| NOTE ×9 | disabled-button third-click race; S2 whitespace coercion; S3 zone guard (throw path bypasses presence check); presence-check scope comment; silent stability-wait exhaustion (+request-start semantics, apiLatencyMs caveat); E1-retry panel guard; E2 err-path restore; panel restore not load-bearing (documented); tool.action catch removes exportBookmarks' pageerror channel (kept — live UX correct; debt note updated); modules reference-copy label stale | all fixed or recorded as stated |

Post-fix: 85/85 across the three virtualized entries (36 on the index entry — the new
uuid-observed assertion included), syntax clean, exact E2 expectation `header=7 (want
exactly 7)` green.

## Verdict and stop rationale (DEC-029)

Round 1: 17 verified findings, all pre-existing relative to the round. Round 2: 3 WARNs +
9 NOTEs, **100% loop-introduced** (defects in round 1's fixes). Provenance has fully
flipped, which is the DEC-029 stop signal — a further identical round would be reviewing
its own edits. The loop stops here: round-2 findings fixed and directly verified (green
run, exact expectations, mutation evidence where the lenses supplied it), full matrix
green on both engines, and the PR's GitHub Codex cycle is the independent downstream gate
this tier exists to feed.

**PIPELINE: APPROVED** — 3 tiers run; 2 CRITICAL + 7 WARN + ~21 NOTE found across two
Tier 3 rounds, 0 false positives, all fixed or explicitly scoped on the record.

## Tier 3 backend experiment record (per opus.md)

backend=opus (5 lenses round 1 + 3 validation lenses round 2, skeptic duty shared with
operator source-verification) · findings 31 raw r1 + 20 raw r2 / 29 verified total / 0
false positives · 2 CRITICAL, 7 WARN · token cost ≈ 706k (r1) + 480k (r2) subagent tokens ·
**GitHub rounds needed afterwards: 0 fix rounds.** Codex reviewed `3ef6239` twice — the
automatic on-open review (👍, no suggestions) and one explicitly forced re-review
("Didn't find any major issues") — with zero findings of any severity. For comparison,
v12.0 needed 24 GitHub rounds and v12.1 needed 7. This is the metric the backend
experiment exists to measure, and on this PR the opus tier fully pre-empted the
downstream loop.
