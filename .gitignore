# OS files
.DS_Store
Thumbs.db

# Editor files
*.swp
*.swo
.idea/
.vscode/

# Node (if you ever add build tools later)
node_modules/

# Test artifacts (generated by --screenshots flag)
tests/screenshots/

# Playwright output directory
test-results/

# Probe build artifacts and measurement records (probes/ commits only sources)
probes/acn-perf-probe.user.js
probes/results/

# Generated tokenizer variant builds (probes/build-tokenizer-variants.js). Six ~640KB
# near-copies of the userscript; the documented invocation writes to /tmp, but the
# script defaults to this path, so a no-arg run must not litter the repo (Tier 3, PR #70).
probes/variants/
